# Fish Speech 语音克隆使用指南

本指南详细介绍如何使用 Fish Speech 进行语音克隆，包括 Python 代码调用、参数设置和最佳实践。

## 语音克隆原理

Fish Speech 的语音克隆基于以下技术：
- **参考音频编码**：将参考音频转换为 VQ 语义标记
- **上下文学习**：使用参考文本和音频作为提示，生成相似音色的语音
- **零样本学习**：无需训练即可克隆新的声音

## 准备工作

### 1. 环境准备
```bash
# 安装依赖
pip install -e .

# 下载模型（如果还没下载）
huggingface-cli download fishaudio/openaudio-s1-mini --local-dir checkpoints/openaudio-s1-mini
```

### 2. 准备参考音频
- **格式要求**：WAV、MP3、FLAC 等常见格式
- **时长建议**：10-30 秒（最低不少于 3 秒）
- **内容建议**：清晰的语音，包含丰富的音素变化
- **文本对应**：需要准备与音频内容完全一致的文本

## Python 代码调用语音克隆

### 直接调用推理引擎

```python
import torch
import queue
from pathlib import Path
from fish_speech.inference_engine import TTSInferenceEngine
from fish_speech.models.text2semantic.inference import launch_thread_safe_queue
from fish_speech.models.dac.inference import load_model as load_decoder_model
from fish_speech.utils.schema import ServeTTSRequest, ServeReferenceAudio

# 1. 初始化模型
llama_queue = launch_thread_safe_queue(
    checkpoint_path="checkpoints/openaudio-s1-mini",
    device="cuda",
    precision=torch.bfloat16,
    compile=True,  # 启用编译加速
)

decoder_model = load_decoder_model(
    config_name="modded_dac_vq",
    checkpoint_path="checkpoints/openaudio-s1-mini/codec.pth",
    device="cuda",
)

# 2. 创建推理引擎
engine = TTSInferenceEngine(
    llama_queue=llama_queue,
    decoder_model=decoder_model,
    precision=torch.bfloat16,
    compile=True,
)

# 3. 准备参考音频
with open("reference_audio.wav", "rb") as f:
    reference_audio_bytes = f.read()

# 4. 构建请求
request = ServeTTSRequest(
    text="这是要合成的文本内容",
    references=[
        ServeReferenceAudio(
            audio=reference_audio_bytes,
            text="对应的参考音频文本内容"
        )
    ],
    max_new_tokens=1024,
    chunk_length=200,
    top_p=0.8,
    repetition_penalty=1.1,
    temperature=0.7,
    format="wav",
    seed=42,  # 固定种子以获得可重复结果
    use_memory_cache="on"
)

# 5. 执行推理
results = list(engine.inference(request))
final_result = results[-1]  # 获取最终结果

if final_result.code == "final":
    sample_rate, audio_data = final_result.audio
    # 保存音频
    import scipy.io.wavfile as wavfile
    wavfile.write("output.wav", sample_rate, audio_data)
    print("语音克隆完成！")
else:
    print(f"错误：{final_result.error}")
```

## 语音克隆参数详解

### ServeTTSRequest 参数说明

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `text` | str | 必填 | 要合成的文本内容 |
| `references` | list | [] | 参考音频列表，用于语音克隆 |
| `reference_id` | str | None | 预存参考音频的ID，优先级高于references |
| `max_new_tokens` | int | 1024 | 最大生成token数量 |
| `chunk_length` | int | 200 | 分块长度，影响生成质量 |
| `top_p` | float | 0.8 | 核采样参数，控制多样性 |
| `repetition_penalty` | float | 1.1 | 重复惩罚，避免重复 |
| `temperature` | float | 0.8 | 温度参数，控制随机性 |
| `format` | str | "wav" | 输出格式：wav、pcm、mp3 |
| `seed` | int | None | 随机种子，固定可获得重复结果 |
| `use_memory_cache` | str | "off" | 是否缓存参考音频编码结果 |

### 参考音频参数

```python
class ServeReferenceAudio:
    audio: bytes  # 音频文件的字节数据
    text: str     # 与音频完全对应的文本内容
```

## 高级用法

### 1. 使用预存参考音频

```python
# 1. 创建参考音频目录
import os
ref_dir = "references/my_voice"
os.makedirs(ref_dir, exist_ok=True)

# 2. 复制参考音频到目录
import shutil
shutil.copy("my_voice.wav", f"{ref_dir}/audio1.wav")

# 3. 创建对应的文本文件
with open(f"{ref_dir}/audio1.lab", "w", encoding="utf-8") as f:
    f.write("这是参考音频的文本内容")

# 4. 使用 reference_id 进行语音克隆
request = ServeTTSRequest(
    text="使用预存参考音频进行语音克隆",
    reference_id="my_voice",  # 使用目录名作为ID
    use_memory_cache="on"
)
```

### 2. 多参考音频增强

```python
# 使用多个参考音频提高克隆质量
request = ServeTTSRequest(
    text="使用多个参考音频进行语音克隆",
    references=[
        ServeReferenceAudio(audio=audio1, text="第一段参考文本"),
        ServeReferenceAudio(audio=audio2, text="第二段参考文本"),
        ServeReferenceAudio(audio=audio3, text="第三段参考文本"),
    ]
)
```

### 3. 语音风格控制

```python
# 通过参数调整语音风格
request = ServeTTSRequest(
    text="调整语音风格的示例",
    references=[reference],
    temperature=0.6,  # 较低值：更稳定，较高值：更多样
    top_p=0.9,        # 较高值：更多变化
    repetition_penalty=1.2,  # 防止重复
    chunk_length=150  # 较小的块：更精细的控制
)
```

## 性能优化

### 1. 内存缓存优化

```python
# 启用内存缓存避免重复编码
request = ServeTTSRequest(
    text="缓存优化示例",
    references=[reference],
    use_memory_cache="on"  # 缓存参考音频编码结果
)

# 清除缓存（如果需要）
# 重启服务或调用内存管理API
```

### 2. 编译加速

```python
# 在初始化时启用torch.compile
llama_queue = launch_thread_safe_queue(
    checkpoint_path="checkpoints/openaudio-s1-mini",
    device="cuda",
    precision=torch.bfloat16,
    compile=True,  # 启用编译，提升10倍速度
)
```

## 常见问题解决与优化方案

### 1. 语音克隆不像的优化方案

当发现克隆的语音与参考音色差异较大时，可以按以下步骤优化：

#### 参考音频优化
- **最佳时长**：15-25秒，包含完整的语义单元
- **内容选择**：包含丰富的音调变化和情感表达
- **录音环境**：安静环境，采样率16kHz以上，单声道
- **避免问题**：
  - 避免背景音乐或噪音
  - 避免过度压缩的音频
  - 避免语速过快或过慢

#### 数据增强策略
```python
# 使用多个参考音频增强克隆效果
optimization_config = {
    "multi_reference": True,
    "reference_audio_paths": [
        "ref_1.wav",  # 正常语速
        "ref_2.wav",  # 稍快语速  
        "ref_3.wav",  # 稍慢语速
    ],
    "reference_texts": [
        "第一段参考文本",
        "第二段参考文本",
        "第三段参考文本"
    ]
}

# 构建增强请求
request = ServeTTSRequest(
    text="要合成的文本",
    references=[
        ServeReferenceAudio(
            audio=audio_to_bytes(path), 
            text=text
        )
        for path, text in zip(
            optimization_config["reference_audio_paths"],
            optimization_config["reference_texts"]
        )
    ],
    temperature=0.5,  # 降低随机性增强稳定性
    repetition_penalty=1.3,  # 提高重复惩罚
    top_p=0.7  # 降低多样性
)
```

#### 参数调优策略
```python
# 音色保真度优先的参数组合
high_fidelity_params = {
    "temperature": 0.4,        # 极低温度确保稳定性
    "top_p": 0.6,             # 严格限制采样范围
    "repetition_penalty": 1.4, # 强重复惩罚避免走音
    "chunk_length": 150,      # 较小块长提高精度
    "max_new_tokens": 800     # 适当限制避免过度生成
}

# 逐步优化流程
def optimize_cloning_quality(text, reference_audio, reference_text):
    """逐步优化克隆质量"""
    
    # 第1步：基础参数
    base_params = high_fidelity_params.copy()
    
    # 第2步：多次尝试取最佳
    best_audio = None
    best_similarity = 0
    
    for seed in [42, 123, 456, 789, 1011]:
        request = ServeTTSRequest(
            text=text,
            references=[ServeReferenceAudio(
                audio=reference_audio,
                text=reference_text
            )],
            seed=seed,
            **base_params
        )
        
        # 这里可以添加音色相似度评估
        audio = generate_audio(request)
        similarity = evaluate_voice_similarity(audio, reference_audio)
        
        if similarity > best_similarity:
            best_similarity = similarity
            best_audio = audio
    
    return best_audio
```

#### 高级优化技巧
- **音频预处理**：使用降噪、音量标准化
- **文本对齐**：确保参考文本与音频100%时间对齐
- **风格标记**：在文本中添加情感标记
- **分段处理**：长文本分段，每段使用相同参数

### 2. 控制克隆语音时长的方法

#### 时长估算原理
Fish Speech的语音时长主要由以下因素决定：
- 文本长度（字符数量）
- 语速参数（chunk_length）
- 语言特性（中文约4-5字符/秒）

#### 精确时长控制

```python
def calculate_expected_duration(text, chars_per_second=4.5):
    """
    估算语音时长（秒）
    中文：4.5字符/秒，英文：5字符/秒，混合：4.2字符/秒
    """
    chinese_chars = sum(1 for c in text if '\u4e00' <= c <= '\u9fff')
    english_chars = sum(1 for c in text if c.isalpha())
    other_chars = len(text) - chinese_chars - english_chars
    
    expected_duration = (
        chinese_chars / 4.5 + 
        english_chars / 5.0 + 
        other_chars / 4.0
    )
    
    return expected_duration

def control_synthesis_duration(text, target_duration, reference_audio, reference_text):
    """
    精确控制合成语音时长
    """
    
    # 计算期望时长
    expected = calculate_expected_duration(text)
    
    # 计算时长比例
    ratio = target_duration / expected
    
    # 调整chunk_length控制语速
    # chunk_length与语速成反比
    base_chunk = 200
    adjusted_chunk = int(base_chunk / ratio)
    adjusted_chunk = max(100, min(300, adjusted_chunk))  # 限制在有效范围内
    
    # 根据时长调整max_new_tokens
    chars_per_token = 0.8  # 经验值
    adjusted_tokens = int(len(text) / chars_per_token * ratio)
    
    request = ServeTTSRequest(
        text=text,
        references=[ServeReferenceAudio(
            audio=reference_audio,
            text=reference_text
        )],
        max_new_tokens=adjusted_tokens,
        chunk_length=adjusted_chunk,
        temperature=0.6,  # 较低温度确保稳定
    )
    
    return request

# 使用示例
text = "这是一段需要精确控制时长的文本内容"
target_duration = 3.5  # 目标时长3.5秒

request = control_synthesis_duration(
    text=text,
    target_duration=target_duration,
    reference_audio=ref_audio_bytes,
    reference_text="参考文本"
)
```

#### 实时时长调整
```python
class DurationController:
    """实时语音时长控制器"""
    
    def __init__(self):
        self.speed_ratios = {
            'very_slow': 0.7,
            'slow': 0.85,
            'normal': 1.0,
            'fast': 1.2,
            'very_fast': 1.4
        }
    
    def adjust_speed(self, text, speed_level='normal', reference_audio=None, reference_text=None):
        """根据语速等级调整参数"""
        
        ratio = self.speed_ratios.get(speed_level, 1.0)
        base_chunk = 200
        
        # 计算调整后的参数
        adjusted_chunk = int(base_chunk / ratio)
        adjusted_chunk = max(100, min(300, adjusted_chunk))
        
        # 温度调整：慢速需要更低温度，快速需要更高
        temperature = max(0.4, min(0.9, 0.7 / ratio))
        
        request = ServeTTSRequest(
            text=text,
            references=[ServeReferenceAudio(
                audio=reference_audio,
                text=reference_text
            )] if reference_audio else [],
            max_new_tokens=int(len(text) * 1.2 / ratio),
            chunk_length=adjusted_chunk,
            temperature=temperature,
            top_p=0.8 if ratio > 1 else 0.7  # 快速时更严格的采样
        )
        
        return request

# 使用示例
controller = DurationController()
request = controller.adjust_speed(
    text="需要调整语速的文本",
    speed_level='slow',  # 慢速
    reference_audio=ref_audio,
    reference_text="参考文本"
)
```

#### 后处理时长校准
```python
def post_process_duration(audio_data, sample_rate, target_duration):
    """
    后处理时长校准
    """
    current_duration = len(audio_data) / sample_rate
    
    if abs(current_duration - target_duration) > 0.2:  # 超过200ms才调整
        import librosa
        
        # 使用librosa进行时间拉伸
        stretch_ratio = target_duration / current_duration
        adjusted_audio = librosa.effects.time_stretch(
            audio_data.astype(float), 
            rate=stretch_ratio
        )
        
        return adjusted_audio.astype(audio_data.dtype)
    
    return audio_data
```

### 3. 音频质量不佳
- **检查参考音频质量**：确保清晰、无噪音
- **调整参数**：降低temperature，提高repetition_penalty
- **增加参考音频时长**：10-30秒最佳

### 4. 音色不一致
- **确保文本对应**：参考文本必须与音频完全一致
- **使用多个参考**：3-5个不同内容的参考音频
- **固定随机种子**：设置seed参数

### 5. 内存不足
- **减少批量大小**：一次处理较少文本
- **清理缓存**：定期重启服务
- **使用CPU模式**：在GPU内存不足时

## 最佳实践

1. **参考音频选择**：
   - 选择发音清晰、无背景噪音的音频
   - 包含丰富的音调和语速变化
   - 避免包含音乐或其他声音

2. **文本准备**：
   - 参考文本必须与音频内容100%匹配
   - 包含标点符号和停顿
   - 使用标准语法

3. **参数调优**：
   - 质量优先：temperature=0.6-0.7, top_p=0.8-0.9
   - 多样性优先：temperature=0.8-0.9, top_p=0.9-1.0
   - 稳定性优先：固定seed，降低temperature

4. **批量处理**：
   - 预编码参考音频（use_memory_cache="on"）
   - 使用异步处理提高效率
   - 合理设置chunk_length平衡质量和速度